<h3>
  $<%= @lamp.price %> is a small price to pay for a <%= @lamp.name %> Lamp!
</h3>

<% if @error %>
  <h4 class="error">
    <%= @error %>
  </h4>
<% end %>

<% if @lamp.quantity > 0 %>

  <form id="buy-lamp" action="/buy/<%= @lamp.id %>" method="post">
    <label for="price"><%= @lamp.name %> Lamp:</label>
    <input id="price" type="text" name="price" value="$<%= @lamp.price %>" readonly disabled>
    <input type="submit" value="Buy">
  </form>

<% else %>
  <p>Sorry, we don't have any more <%= @lamp.name %> Lamps in stock.</p>
<% end %>


<a href="/lamps/<%= @lamp.id %>">Back</a>
